name: Label PR

# SECURITY: this workflow runs with write access to the base repo on fork PRs,
# because `pull_request_target` executes in the context of the base branch.
# Keep it metadata-only — do NOT add actions/checkout or any step that
# executes PR-supplied content (install scripts, build commands, etc.).
# See https://securitylab.github.com/resources/github-actions-preventing-pwn-requests/
on:
  pull_request_target:
    types: [opened, edited]

jobs:
  label:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            const body = context.payload.pull_request.body || '';
            const labels = [];

            if (body.includes('[x] **Feature**'))             labels.push('PR: Feature');
            else if (body.includes('[x] **Infrastructure**')) labels.push('PR: Infrastructure');
            else if (body.includes('[x] **Verify**'))         labels.push('PR: Verify');
            else if (body.includes('[x] **Fix**'))            labels.push('PR: Fix');
            else if (body.includes('[x] **Refactor**'))       labels.push('PR: Refactor');
            else if (body.includes('[x] **Docs**'))           labels.push('PR: Docs');

            if (body.includes('biblio-claw-pr-template: v1')) labels.push('follows-guidelines');

            if (labels.length > 0) {
              await github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.payload.pull_request.number,
                labels,
              });
            }
