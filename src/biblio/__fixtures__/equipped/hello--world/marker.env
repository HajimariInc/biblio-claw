# biblio-claw M3 Phase 2 決定的マーカー nonce の単一正本 (single source of truth)
# 全アサーション (biblio-equip-spawn-verify.ts / verify-m3-phase-2.sh など) は
# この値を参照する。この文字列がコンテナの出力に現れる = skill がロードされ
# 発火し payload が実行された証拠。
# 注意: この nonce はプロンプトには書かない。SKILL.md / emit-marker.sh 側にのみ置く。
MARKER=BIBLIO_EQUIP_M3_P2_MARKER_3f8f6e65
